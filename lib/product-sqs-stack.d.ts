import { Stack, StackProps } from 'aws-cdk-lib/core/lib/stack';
import { Construct } from 'constructs';
export declare class ProductSqsStack extends Stack {
    constructor(scope: Construct, id: string, props?: StackProps);
}
